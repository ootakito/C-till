class ABC
{
    static void Main()
    {
        int[] numbers = new int[3]; // 要素数3の配列を作成
        numbers[0] = 10;            // 1番目の要素に10を代入
        numbers[1] = 20;            // 2番目の要素に20を代入
        numbers[2] = 30;            // 3番目の要素に30を代入

        Console.WriteLine(numbers[0]); // 出力: 10
        Console.WriteLine(numbers[1]); // 出力: 20
        Console.WriteLine(numbers[2]); // 出力: 30





    }
}

int[] numbers = new int[3];ここで配列の数を作成する３はインデックス番号で０１２になる。


class ABC
{
    static void Main()
    {
        int[] numbers = new int[5]; // 5つの数値を格納する配列

        for (int i = 0; i < numbers.Length; i++)
        {
            while (true)
            {
                Console.WriteLine($"{i + 1}つ目の数値を入力してください:");
                string input = Console.ReadLine();

                if (int.TryParse(input, out int value))
                {
                    numbers[i] = value; // 配列に値を格納
                    break; // 正常な入力ならループを抜ける
                }
                else
                {
                    Console.WriteLine("不正な入力です。数値を入力してください。");
                }
            }
        }

        // 合計と平均を計算
        int sum = 0;
        for (int i = 0; i < numbers.Length; i++)
        {
            sum += numbers[i];
        }
        double average = (double)sum / numbers.Length;

        Console.WriteLine($"合計: {sum}");
        Console.WriteLine($"平均: {average:F2}");
    }
}

 for (int i = 0; i < numbers.Length; i++)　コンソールアプリでlengthが使えるのは、
何もしてなくても予めusingが内部に入ってて見えないから

{average:F2}のF２は小数点２位という意味になる。
double average = (double)sum / numbers.Length;は
intをキャストしてdoubleにしてる。(double)でキャスト


class ABC
{
    static void Main()
    {
        int[] numbers = new int[5];

        for (int i = 0; i < numbers.Length; i++)
        {
            while (true)
            {
                Console.WriteLine($"{i + 1}つ目の数値を入力してください:");
                string input = Console.ReadLine();

                if (int.TryParse(input, out int value))
                {
                    numbers[i] = value;
                    break;
                }
                else
                {
                    Console.WriteLine("不正な入力です。数値を入力してください。");
                }
            }

        }

        int sum = 0;

        for (int i = 0; i < numbers.Length; i++)
        {
            sum += numbers[i];
        }

        double avg = (double)sum / numbers.Length;

        for (int i = 0; i < numbers.Length-1; i++)
        {
            for (int j = 0; j < numbers.Length -1; j++)
            {
                if (numbers[j] > numbers[j + 1])
                {
                    int temp = numbers[j];
                    numbers[j] = numbers[j + 1];
                    numbers[j + 1] = temp;
                }
            }
        }

        int median = numbers[numbers.Length / 2];

        Console.WriteLine($"平均値: {avg:F2}");
        Console.WriteLine($"中央値: {median}");
    }
}


　　　　　　　　　　　int temp = numbers[j];
                    numbers[j] = numbers[j + 1];
                    numbers[j + 1] = temp;

コードが順に実行される理由で成立する仕組み

1. 一時保存（temp）


int temp = numbers[j];

まず、左側の値（numbers[j]）を一時的に変数 temp に保存します。
目的: 次の操作で numbers[j] を上書きしても、元の値が失われないようにする。

2. 右側の値を左側にコピー


numbers[j] = numbers[j + 1];

左側の値を、右側の値で上書きします（つまり、右側の値を左側に移動）。
結果: 左側が上書きされ、右側と同じ値になります。

3. 一時保存した値を右側にコピー


numbers[j + 1] = temp;

最初に保存しておいた左側の値（temp）を右側にコピーします。
結果: 左右の値が入れ替わる。
